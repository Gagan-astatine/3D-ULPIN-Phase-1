# Demo property certificates

Twelve fictional PDF certificates and their complete verification JSON records. The bundled QR codes open http://localhost:5173/. Keep the application running when following a code. A phone needs a reachable LAN or hosted app URL. Regenerate with `npm run certificates -- https://your-app.example/` or set Application URL inside each in-app certificate dialog. These documents are not legal ownership certificates or government records.
